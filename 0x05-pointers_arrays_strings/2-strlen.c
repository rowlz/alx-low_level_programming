#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - swaps values of two integers
 * @s: string
 * Return: Always 0
*/

int _strlen(char *s)
{
	char new_string[] = s;
	int a;

	for (a = 0; new_string[a] != '\0'; ++a)
	{

	}
	return (a);
}
