#include <unistd.h>
#include "main.h"

/**
 * add - The start of program
 * @x: input
 * @y: input
 * Description : prints out the alphabet in lower case
 * Return: 1 and print '+'if n greater than zero
 * 0 and print '0' if zero
 * -1 and prints '-' if less than zero
*/

int add(int x, int y)
{
	int sum;

	sum = x + y;

	return (sum);

}
