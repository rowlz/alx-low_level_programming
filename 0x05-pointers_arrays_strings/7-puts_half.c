#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * puts_half - swaps values of two integers
 * @str: string
 * Return: Always 0
*/

void puts_half(char *str)
{
	int a;

	for (a = 0; str[a] != '\0'; a++)
	{

	}

	for (i /= 2; str[a] != '\0'; a++)
	{
		_putchar(str[a]);
	}

	_putchar('\n');
}
